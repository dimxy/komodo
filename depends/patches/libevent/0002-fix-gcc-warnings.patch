--- a/util-internal.h
+++ b/util-internal.h
@@ -306,1 +306,1 @@
-int evutil_ersatz_socketpair_(int, int , int, evutil_socket_t[]);
+int evutil_ersatz_socketpair_(int, int , int, evutil_socket_t fd[2]);
--- a/event.c
+++ b/event.c
@@ -1354,23 +1354,20 @@ static inline void
 event_signal_closure(struct event_base *base, struct event *ev)
 {
-	short ncalls;
 	int should_break;
 
 	/* Allows deletes to work */
-	ncalls = ev->ev_ncalls;
-	if (ncalls != 0)
-		ev->ev_pncalls = &ncalls;
+	if (ev->ev_ncalls != 0)
+		ev->ev_pncalls = &ev->ev_ncalls;
 	EVBASE_RELEASE_LOCK(base, th_base_lock);
-	while (ncalls) {
-		ncalls--;
-		ev->ev_ncalls = ncalls;
+	while (ev->ev_ncalls) {
+		ev->ev_ncalls--;
-		if (ncalls == 0)
+		if (ev->ev_ncalls == 0)
 			ev->ev_pncalls = NULL;
 		(*ev->ev_callback)(ev->ev_fd, ev->ev_res, ev->ev_arg);
 
 		EVBASE_ACQUIRE_LOCK(base, th_base_lock);
 		should_break = base->event_break;
 		EVBASE_RELEASE_LOCK(base, th_base_lock);
 
 		if (should_break) {
-			if (ncalls != 0)
+			if (ev->ev_ncalls != 0)
 				ev->ev_pncalls = NULL;
 			return;
 		}
 	}
 }
